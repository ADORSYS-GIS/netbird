# ========================================
# Keycloak Ansible Inventory
# ========================================

# ----------------------------------------
# LOCAL TESTING (runs on your local machine)
# ----------------------------------------
# Uncomment this section to test locally
[keycloak]
localhost ansible_connection=local

[keycloak:vars]
# No SSH needed for localhost - runs commands directly
# ansible_python_interpreter=/usr/bin/python3  # Uncomment if needed

# ----------------------------------------
# REMOTE HOST (runs on another machine)
# ----------------------------------------
# Uncomment this section for remote deployment
# [keycloak]
# 192.168.1.229

# [keycloak:vars]
# ansible_user=usherking
# ansible_ssh_private_key_file=~/.ssh/id_rsa  # If using SSH key
# ansible_become=yes  # If need sudo privileges
# ansible_become_password="{{ vault_ansible_become_password }}"  # Use vault for sudo password

