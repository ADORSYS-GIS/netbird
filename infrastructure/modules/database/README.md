# Database Module

Unified database configuration module supporting multiple engines.

## Supported Engines

- **SQLite**: Local file-based database (default).
- **PostgreSQL**: Production-grade SQL database (recommended for HA).
- **MySQL**: Alternative SQL database support.

## Usage

Configure via `database_type` variable in main configuration.
