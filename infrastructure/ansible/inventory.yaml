---
all:
  children:
    netbird_servers:
      hosts:
        netbird-primary:
          ansible_host: <YOUR_DOMAIN>
          ansible_user: ubuntu
          ansible_connection: ssh         
          ansible_ssh_private_key_file: /path/to/your/ssh/key
