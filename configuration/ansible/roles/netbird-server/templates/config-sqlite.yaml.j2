# /etc/netbird/config.json (SQLite)
# Auto-generated by Ansible

{
    "ServerConfig": {
        "HttpConfig": {
            "Address": "0.0.0.0:80"
        },
        "StoreConfig": {
            "Engine": "sqlite",
            "StoreDir": "{{ sqlite_database_path | dirname }}"
        },
        "Auth0Config": {
            "Issuer": "{{ keycloak_url }}/realms/{{ keycloak_realm }}",
            "Audience": "{{ keycloak_client_id }}",
            "ClientID": "{{ keycloak_client_id }}",
            "ClientSecret": "{{ keycloak_client_secret }}",
            "IdpProvider": "keycloak"
        },
        "PKCEAuthorizationFlow": {
            "Enabled": true
        }
    },
    "ManagementURL": "https://{{ netbird_domain }}",
    "SignalConfig": {
        "URI": "https://{{ netbird_domain }}:443"
    },
    "TurnConfig": {
        "TimeBasedSecret": "{{ coturn_password }}",
        "Turns": [
            {
                "Proto": "udp",
                "URI": "turn:{{ netbird_domain }}:3478",
                "Username": "netbird",
                "Password": "{{ coturn_password }}"
            }
        ]
    }
}
