---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install common packages
  apt:
    name:
      - curl
      - wget
      - git
      - htop
      - ufw
      - chrony
    state: present

# Include detailed firewall rules
- include_tasks: firewall.yml
